defmodule PromptOn.Repo.Migrations.AddEvalsIdentityAndRetentionIndex do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create unique_index(:evaluation_runs, [:project_id, :deployment_id],
             name: "evaluation_runs_one_active_run_per_deployment_index",
             where: "(status IN ('queued', 'running'))"
           )
  end

  def down do
    drop_if_exists unique_index(:evaluation_runs, [:project_id, :deployment_id],
                     name: "evaluation_runs_one_active_run_per_deployment_index"
                   )
  end
end
